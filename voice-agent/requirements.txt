# No [anthropic] extra. It is the only thing that installs the Anthropic SDK
# (METADATA: `anthropic~=0.49.0; extra == "anthropic"`), and phase 7 removed the
# last import of it — src/pipeline/services.py builds OpenAILLMService against
# the gateway. Unimported is not uninstalled: while the extra was here,
# `import anthropic` still succeeded in the image (0.49.0), so a direct client
# could be typed back in and no source grep would notice.
# No [openai] extra is needed either — `openai` is a CORE pipecat dependency
# (`Requires-Dist: openai<3,>=1.74.0`, no marker). The extra only adds websockets
# support for the Realtime service, which this agent does not use.
pipecat-ai[deepgram,webrtc]==0.0.102
fastapi==0.135.1
starlette>=0.52.1
uvicorn[standard]==0.34.0
httpx>=0.27.0
pydantic-settings>=2.0.0
Pillow>=11.1.0,<12
supabase>=2.0.0
structlog>=24.0.0
# intake-core is vendored into the image at /var/task/intake_core via Dockerfile COPY.
# Local dev: pip install -e ../intake-core
