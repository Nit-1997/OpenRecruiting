"""Phase 2: Knit-wire-specific translation helpers.

OpenRecruiting-model mapping adapters (canonical ↔ requisitions/candidates) will live
at ats/ level so passthrough providers reuse them — NOT here. See spec §2.
"""
