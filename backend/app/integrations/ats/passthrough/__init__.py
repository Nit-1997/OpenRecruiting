"""Phase 2+: per-ATS native providers (ashby/, workable/) implementing the
same core ports for capabilities Knit's unified model lacks (scorecard
submit, interview-schedule writes, Workable requisitions)."""
