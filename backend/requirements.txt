fastapi==0.135.1
starlette>=0.52.1
uvicorn[standard]==0.27.0
python-dotenv==1.0.0
pydantic[email]>=2.10.0
pydantic-settings>=2.1.0
PyJWT[crypto]>=2.8.0
httpx>=0.24.0,<0.28.0
boto3>=1.34.0
# Streaming only. The nine non-streaming call sites moved to the LiteLLM gateway
# via llm-core in phase 2; text_runner and debrief_chat/runner still stream
# then breaks two routers at import.
# Dodo Payments SDK — required by admin billing_service (ported from v1)
dodopayments>=1.84.0
# Jinja2 — email template rendering, required by services/email (activated by feedback portal)
Jinja2>=3.0
# Intake JD extract pipeline
beautifulsoup4>=4.12.0
pypdf>=4.0.0
python-docx>=1.1.0
python-multipart>=0.0.9
