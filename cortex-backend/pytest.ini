[pytest]
# Markers only. Deliberately no asyncio_mode: the suite is written against
# pytest-asyncio strict mode with explicit @pytest.mark.asyncio, and switching
# to auto would change collection for every test here.
markers =
    live_infra: needs a real Neo4j/Supabase; excluded from the hermetic `make test-cortex` run
